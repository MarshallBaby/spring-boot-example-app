name: Build project
on: push

jobs:

  build:
    runs-on: ubuntu-20.04
    steps:

      - name: List files
        run:
          ls ${{ github.workspace }} > ${{ files }}

      - name: Aboba
        run:
          cat ${{ files }}